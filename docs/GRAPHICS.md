I updated the Graphics docs with info about the new visual themes, texture sources, and how to customize backgrounds. Themes are switchable from the new selector in the UI and persisted into settings.json.
